---
paths:
  - "**/*.py"
  - "**/pyproject.toml"
---

Use type hints on all function signatures. Prefer `pathlib.Path` over `os.path`. Use dataclasses or Pydantic models for structured data. Prefer list/dict comprehensions over map/filter.
